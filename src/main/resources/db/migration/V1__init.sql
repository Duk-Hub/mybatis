CREATE TABLE jpa_data
(
	id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	title      VARCHAR(255) NOT NULL,
	content    VARCHAR(1000) NOT NULL,
	is_deleted BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE TABLE mybatis_data
(
	id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	title      VARCHAR(255) NOT NULL,
	content    VARCHAR(1000) NOT NULL,
	is_deleted BOOLEAN NOT NULL DEFAULT FALSE
);